import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1bb0a5",
      light: " skyblue",
    },
    secondary: {
      main: "#15c630",
    },
    otherColor: {
      main: "#15dd22",
    },
  },
});
